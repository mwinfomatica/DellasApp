enum DialogType { success, warning, info, error }

enum EnumTipoPedido { vandaRapida, vendaMesa, novaMesa }

enum EnumStatusPedido {
  emAndamento,
  finalizado,
  cancelado,
  pendeteSincronizacao
}

enum EnumFormaPgtoPedido { cartaoCredito, cartaoDebito, dinheiro, pix }

enum EnumTipoParametro {
  int,
  bool,
  string,
  dateTime,
  decimal,
}
